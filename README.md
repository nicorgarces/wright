# wright

Short description: replace this with a one-line description of your project.

How to run locally:
- Open the project folder in VS Code.
- Install dependencies if needed:
  - For Node: `npm install`
  - For .NET: `dotnet restore`
- Start the app:
  - For Node: `npm start` (or the script your project uses)
  - For .NET: `dotnet run` or open in Visual Studio

Notes:
- Remove secrets before making the repository public.
- Add any environment variables or setup steps required here.